# CLI

No end-user CLI is part of the web MVP.

Current interface is browser + HTTP API (`/api/generate`).

Future option: add a lightweight CLI wrapper that calls the same generation core for parity with automation flows.
